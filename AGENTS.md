# AGENTS.md

本文件适用于仓库中的全部文件。修改本博客前，应先阅读本文件以及相关布局和样式源文件。

## 项目概况

- 站点：`ListenSnow's Blog`
- 地址：<https://listensnow405.github.io/>
- 技术栈：GitHub Pages、Jekyll、Minima、Liquid、原生 JavaScript
- 主要语言：简体中文；少量英文只用于眉题、状态文字和技术氛围
- 样式入口：`assets/main.scss`
- 全局布局：`_layouts/default.html`
- 首页：`index.html`
- 部署方式：推送到 `main` 后由 GitHub Pages 自动构建

不要把项目迁移到 React、Vue、Next.js 等框架，也不要引入需要额外构建服务的依赖，除非用户明确要求更换技术栈。

## 本地 Jekyll 环境

当前 Windows 开发环境已经配置完成：

- RubyInstaller + Devkit：`3.1.7-1`，安装目录为 `C:\Ruby31-x64`。
- Ruby：`3.1.7p261`，平台为 `x64-mingw-ucrt`。
- RubyGems：`3.3.27`。
- Bundler：`2.3.27`。
- `github-pages`：`232`。
- Jekyll：`3.10.0`。
- Minima：`2.5.1`。
- Ruby 版本同时记录在 `.ruby-version` 中；不要在未完成完整构建验证时随意升级 Ruby 主版本或次版本。

项目依赖由根目录的 `Gemfile` 管理。当前机器通过 `.bundle/config` 将 Gem 安装到 `vendor/bundle`；`.bundle/`、`vendor/`、`Gemfile.lock` 和 Jekyll 生成目录均已忽略，不应提交。新环境首次安装时执行：

```powershell
bundle config set --local path vendor/bundle
bundle install
```

常用本地命令：

```powershell
# 执行一次完整构建
bundle exec jekyll build

# 在本机启动预览，浏览器访问 http://127.0.0.1:4000/
bundle exec jekyll serve --host 127.0.0.1 --port 4000
```

安装或升级 Ruby 后应重新打开终端，使用户级 `PATH` 生效。构建时出现 `To use retry middleware with Faraday v2.0+` 是当前 GitHub Pages 依赖组合产生的非致命提示；只要命令退出码为 `0` 且输出包含 `done`，不应将它误判为构建失败。

## 核心视觉方向

当前风格定义为“雾蓝夜色中的冰蓝技术博客”。整体应当冷静、清晰、轻盈，体现计算机、算法和持续探索的气质。

- 深色但不接近纯黑，以雾蓝、藏蓝和轻微紫蓝渐变构成背景。
- 冰蓝色用于主要操作、当前状态和少量高亮信息。
- 卡片依靠背景亮度、细边框和柔和阴影分层，不使用厚重投影。
- 装饰元素应当抽象、低对比度、非功能性，例如网格、细线、圆环、点阵和纵向坐标文字。
- 风格不是高饱和赛博朋克。避免霓虹泛滥、强烈辉光、复杂玻璃拟态和过度动画。
- 页面重点始终是文章和个人信息，装饰不能抢夺正文注意力。

## 颜色系统

颜色变量集中定义在 `assets/main.scss` 的 `:root` 中。新增组件应优先复用变量，不要散落新的近似颜色。

| 用途 | 变量 | 当前值 |
| --- | --- | --- |
| 页面基础背景 | `--bg` | `#101a2b` |
| 抬升背景 | `--bg-raised` | `#17243a` |
| 半透明卡片 | `--surface` | `rgba(26, 42, 67, 0.84)` |
| 实色卡片 | `--surface-solid` | `#1a2a44` |
| 悬停表面 | `--surface-hover` | `#213653` |
| 普通边框 | `--line` | `rgba(178, 207, 241, 0.18)` |
| 强调边框 | `--line-strong` | `rgba(178, 207, 241, 0.32)` |
| 主文字 | `--text` | `#edf5ff` |
| 次级文字 | `--text-soft` | `#b8c7da` |
| 弱化文字 | `--text-faint` | `#8294ad` |
| 主强调色 | `--accent` | `#7dd3fc` |
| 强调色 | `--accent-strong` | `#38bdf8` |
| 辅助紫蓝 | `--accent-violet` | `#a5b4fc` |
| 在线状态 | `--success` | `#5ee7a8` |

正文区域不要使用纯白大面积背景。警告、错误等语义色可以按需增加，但须保证与现有冷色体系协调。

## 页面背景与两侧装饰

页面背景由多层径向光晕、雾蓝线性渐变和低透明度网格组成：

- 左上角使用冰蓝光晕。
- 右上区域使用轻微紫蓝光晕。
- 页面底部使用非常克制的蓝色光晕。
- 网格仅作为纹理，不得提高到影响正文阅读的强度。

`_layouts/default.html` 中的 `.ambient-edge-left` 和 `.ambient-edge-right` 是宽屏两侧装饰：

- 包含纵向英文标识、细线、微光符号、圆环和点阵。
- 必须保留 `aria-hidden="true"` 和 `pointer-events: none`。
- 仅在宽度不小于 `1200px` 时显示。
- 不得承载导航、状态或任何必须被用户读取的信息。
- 当布局变窄时，应优先隐藏装饰，而不是压缩正文。

## 布局比例

### 首页桌面端

首页通过 `sidebar: true` 启用三栏布局：

1. 左侧个人栏：`220px` 至 `280px`。
2. 中央内容栏：占据全部剩余空间，是宽度调整时的主要受益者。
3. 右侧发现栏：`280px` 至 `340px`。

整体宽度为视口的 `94vw`，最大 `2100px`；栏间距在 `24px` 至 `44px` 之间动态变化。不要重新设置狭窄的固定中央宽度。

### 其他页面桌面端

归档、分类、标签、搜索、关于和文章详情采用左侧个人栏加主内容栏的两栏结构。整体宽度为 `94vw`，最大 `1900px`。

### 响应式规则

- `<= 1100px`：隐藏右侧发现栏，保留左侧栏和主内容。
- `<= 760px`：左侧栏转为顶部粘性导航，页面变为单栏。
- `<= 520px`：按钮、文章卡片、分类卡片和搜索表单进一步适配窄屏。
- 修改样式后必须确认页面没有横向滚动。

## 组件语言

### 左侧个人栏

- GitHub 头像是最主要的视觉识别元素。
- 头像保持圆角方形，不改为完全圆形。
- 绿色小圆点仅表示视觉上的在线/活跃状态。
- 导航使用小型符号加中文标签；当前页使用冰蓝色浅背景。
- GitHub 和 Email 位于底部，保持低调。

### 首页介绍卡片

- 标题是页面最高视觉层级，保持粗体、紧凑字距和两行结构。
- “持续探索”使用冰蓝至紫蓝的渐变文字。
- 英文眉题使用大写、宽字距、小字号。
- 右侧编号、网格和英文短句属于装饰，不应比标题更醒目。

### 文章列表

- 首页文章采用纵向信息流，而不是密集宫格。
- 每篇文章包含序号、分类、标题、摘要、日期、标签和进入箭头。
- 卡片悬停只允许轻微上移、边框增强和背景提亮。
- 标题链接可以覆盖整个卡片，但必须保留键盘焦点可见性。

### 右侧发现栏

- 顺序为搜索、按年归档、分类、标签云。
- 内容由 Jekyll/Liquid 自动生成，不手工维护重复列表。
- 标签与分类使用紧凑的小型胶囊或方圆角标签。

### 内页

- 归档页强调年份和时间线。
- 分类页强调主题卡片及其文章列表。
- 标签页使用标签云和分组文章列表。
- 搜索页保持大输入框、简洁结果卡片和明确的结果数量。
- 文章详情页保持舒适的正文行距、清晰的标题层级、低亮度代码块和可见的分类/标签入口。

## 字体与排版

- 使用系统字体栈，不加载外部网页字体，以保证国内访问速度和稳定性。
- 中文正文行高约为 `1.75` 至 `1.9`。
- 大标题使用较粗字重和轻微负字距。
- 日期、序号、坐标、英文状态等元信息使用系统等宽字体。
- 正文使用 `--text-soft`，标题使用 `--text`，不要让所有文字拥有相同亮度。
- 避免长段居中排版；正文默认左对齐。

## 动效与可访问性

- 常规过渡控制在约 `160ms` 至 `180ms`。
- 悬停位移通常不超过 `4px`，旋转只用于小型箭头等装饰。
- 保留 `:focus-visible` 的清晰焦点环。
- 保留跳转到主要内容的 skip link。
- 所有功能性图片必须提供准确的 `alt`。
- 纯装饰元素应使用 `aria-hidden="true"`。
- 必须继续支持 `prefers-reduced-motion: reduce`。
- 不要用颜色作为表达当前状态的唯一方式。

## 内容与信息架构

文章 front matter 应至少包含：

```yaml
---
layout: post
title: "文章标题"
date: 2026-09-01 12:00:00 +0800
categories: [学习]
tags: [算法, C++, OI]
---
```

- `categories` 和 `tags` 会自动驱动首页侧栏、分类页、标签页和搜索索引。
- 不要在多个页面手工复制分类、标签或年份数据。
- 搜索数据由 `search.json` 自动生成，交互代码位于 `assets/js/search.js`。
- 新增页面链接时使用 Liquid 的 `relative_url`，不要硬编码部署路径。

## 文件职责

- `_config.yml`：站点身份、URL、主题和插件配置。
- `_layouts/default.html`：全局页面骨架和两侧装饰。
- `_layouts/post.html`：文章详情布局。
- `_includes/profile-sidebar.html`：个人信息和主导航。
- `_includes/discover-sidebar.html`：搜索、归档、分类和标签侧栏。
- `_includes/head.html`：SEO、RSS 和带构建版本号的样式引用。
- `assets/main.scss`：全部视觉样式和响应式规则。
- `index.html`：首页介绍和文章信息流。
- `archives.html`、`categories.html`、`tags.html`、`search.html`：内容发现页面。
- `search.json`、`assets/js/search.js`：纯前端站内搜索。

不要删除 `_includes/head.html` 中的 CSS 构建版本号。它用于避免 GitHub Pages/CDN 缓存导致发布后仍显示旧样式。

## 修改原则

- 延续现有视觉系统，而不是为单个页面创造一套新风格。
- 优先修改共享变量和通用组件，避免重复 CSS。
- 保持 Jekyll 和 GitHub Pages 原生兼容，不添加 GitHub Pages 不支持的自定义插件。
- JavaScript 保持原生、轻量、渐进增强；基础内容在脚本失败时仍应可访问。
- 不照搬参考博客的图片、品牌或具体组件，只借鉴信息架构和比例。
- 不随意更改用户的姓名、学校、简介、邮箱、头像地址和站点标题。
- 如果用户只要求视觉调整，不要同时改写博客正文。

## 验证要求

完成改动后，至少执行与变更相关的检查：

1. 检查 YAML/front matter 是否可解析。
2. 对 JavaScript 执行 `node --check assets/js/search.js`。
3. 执行 `git diff --check`。
4. 确认 SCSS 花括号平衡，并让 GitHub Pages 完成真实构建。
5. 检查首页、文章页及受影响的内容发现页面。
6. 对桌面和移动端验证布局，尤其检查横向溢出、导航可用性和文字可读性。
7. 搜索功能变更后，至少用一个现有标题或标签验证能够返回结果。

发布后如页面仍显示旧样式，先确认 HTML 中的 `assets/main.css?v=<build-revision>` 已更新，再判断是否为构建问题。
