# 开发约束

适用于整个仓库。修改前先阅读相关源文件，检查工作区已有改动；保留与任务无关的修改。

站点信息、功能、环境安装、目录结构和发布操作统一见 [README.md](README.md)，本文件只维护开发约束，不重复记录版本号、颜色值或布局尺寸。

## 技术与配置

- 保持 GitHub Pages、Jekyll、Minima、Liquid 和原生 JavaScript 技术栈；未经用户明确要求，不迁移框架，不引入额外构建服务或 Pages 不支持的插件。
- JavaScript 保持轻量、渐进增强，脚本失败时基础内容仍可访问。
- 依赖由 `Gemfile` 管理，Ruby 版本以 `.ruby-version` 为准；升级须完成完整构建验证。不提交被忽略的依赖、锁文件、缓存和构建产物。
- 模板中的站内链接使用 `relative_url`，不硬编码域名或部署路径；保留 `_includes/head.html` 中 CSS 引用的构建版本参数。
- 域名变更须核对 `CNAME`、`_config.yml`、Pages 设置及 DNS 的一致性，并验证 HTTPS；不要仅依据旧文档判断部署状态。

## 内容与实现

- 不擅自修改作者身份、联系方式、头像、站点标题或简介；视觉任务不改写文章正文。
- 文章 front matter 至少包含 `layout`、`title`、`date`、`categories`、`tags`；分类、标签、年份和搜索索引由文章数据自动生成，不手工维护重复列表。
- 保持已发布文章 URL 稳定；如需调整永久链接，处理旧链接兼容。
- 优先复用共享布局、组件及 `assets/main.scss` 中的变量，避免重复样式；借鉴参考站的信息架构，不照搬其品牌、图片或具体组件。

## 文章规划与写作

以下规范沿用 Git 系列已经确定的写作风格，适用于后续技术文章的大纲与正文；用户对具体文章提出的要求优先。

### 标题与大纲

- 文章标题及各级小标题使用简短、准确的名词性短语，接近学术论文的标题表达，如“Git 基础操作与分支管理”“操作分类与影响范围”“重置模式比较”。避免口语化长句、问句、宣传性措辞及“先判断状态，再选择操作”“从第一次提交到团队协作”等叙事式标题。
- 大纲先明确目标读者、必要前置知识、核心问题与内容边界，再安排章节；每节说明要解释的概念、采用的案例和需要掌握的结果，不能只列命令名称。
- 按完整任务或知识主题组织文章，不按原文长度或章节数量机械拆分。兼顾单篇阅读负担与系列数量，避免一篇包揽全部内容，也避免将紧密相关的内容拆得过碎；不强制所有系列采用五篇结构。
- 系列文章区分概念导论与操作专题。导论集中解释基本模型、学习顺序和最小实践；专题深入讲解具体问题。每个知识点确定主要讲解位置，其他文章简短回顾并链接，避免重复铺陈。
- 用户仅要求分析或规划时，只提供方案，不改写正文；按已经确认的大纲生成文章，不擅自扩大主题或改变系列划分。

### 正文表达与讲解深度

- 正文使用准确、自然、克制的书面中文，以清晰解释为主。标题简洁不等于正文晦涩，避免堆砌术语、空泛总结、口号式句子和对话式写作痕迹。
- 通常按“场景引入—必要概念—操作案例—常见问题—速查与练习”展开，允许根据主题调整。开篇简要说明文章解决的问题及阅读前提，不重复长篇背景介绍。
- 常用操作通过完整案例讲透，不写成只有命令列表和一句释义的手册。关键步骤说明前置状态、操作目的、命令作用、预期结果及验证方法；易混淆的操作用同一案例、状态表或关系图比较。
- 系列尽量使用一致的练习项目，同时保证单篇可独立阅读。明确哪些步骤连续执行、哪些是替代方案或独立实验；文件创建、目录切换、占位符替换和环境依赖应在执行相关命令前交代清楚。
- 涉及覆盖文件、删除内容、改写历史等操作时，就近说明影响范围、保留条件与恢复限制。区分本地与共享状态、已提交与未提交内容，不将备份引用或恢复日志描述为万能保障。
- 低频工具采用“适用场景—最小流程—关键限制”的简短介绍，避免为追求全面而扩展成另一篇超长手册。篇幅服务于理解，不为凑字数扩写，也不因压缩篇幅省略关键解释。

### 配套内容与校验

- 教程类文章末尾通常设置“命令速查”“实践练习”“参考资料”；练习应包含可观察的验收标准。系列提供明确的前后篇导航与导读入口，不仅依赖按发布日期生成的相邻文章导航。
- 技术资料优先引用官方文档，并核对容易误解的行为与限制。核心案例应在隔离的练习环境中验证，不在博客源码仓库中执行示例中的重置、清理或历史改写；未验证的部分须如实说明。
- 重构已有文章时保留已发布地址，尽量兼容原章节锚点，并核对正文交叉链接、目录、摘要和搜索索引。详细内容迁移后，原手册可保留为精简速查入口，不重复刊载整套正文。

## 视觉与可访问性

- 延续雾蓝深色背景与冰蓝强调色，以文章阅读为重点；避免大面积纯白、强烈辉光、厚重阴影和过度动画。
- 使用系统字体栈，不加载外部网页字体；保持正文左对齐、清晰的标题层级及文字亮度区分。
- 保留头像的圆角方形、首页纵向文章流和现有响应式布局；窄屏优先隐藏装饰，不挤压正文，不产生页面横向滚动。
- 装饰不得承载必要信息，保留 `aria-hidden="true"`；两侧装饰保留 `pointer-events: none`，仅在宽屏显示。
- 保留键盘操作、可见焦点、跳到正文链接、功能图片的准确 `alt` 和 `prefers-reduced-motion` 支持；状态不得仅靠颜色表达。
- 动效应短促克制；文章目录须保持滚动定位稳定，窄屏可折叠，代码块和表格须适配窄屏阅读。

## 验证与文档

- 所有改动执行 `git diff --check`；纯文档修改核对内容、链接和配置描述，无需重复运行无关构建或页面测试。
- 配置、文章或模板变更检查 YAML/front matter，并执行完整 Jekyll 构建；样式变更同样须通过构建验证。
- JavaScript 变更对受影响脚本执行 `node --check`，并验证对应交互；搜索变更至少用一个现有标题或标签检查结果。
- 页面变更检查首页、文章页和受影响页面的桌面及移动端表现，重点验证横向溢出、导航、目录和可读性。
- 前端视觉改动完成后，提供实际渲染页面的桌面端与移动端截图，在交付回复中直接展示优化效果；截图保存在构建产物或临时目录，不提交到源码仓库。若截图受环境限制无法完成，明确说明原因，不以示意图代替实际截图。
- 发布后确认 GitHub Pages 实际构建与页面结果；缓存排查先检查 CSS 版本参数。未完成的验证须如实说明，不以本地构建代替线上验证。
- 功能、配置或操作流程变化时同步更新 README；开发约束变化才更新本文件。
